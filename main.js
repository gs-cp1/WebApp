// Example JavaScript functionality
document.addEventListener('DOMContentLoaded', () => {
   const message = document.createElement('p');
   message.textContent = 'This message is added by regular JavaScript!';
   document.body.appendChild(message);
});

//creates a variable initialized as a constant. goes into html document using document command and then with the .createelemnt thing  creates whats in the parewntheseis hten next line then takes the message var and uses the .textconetent thing to add a messsage to the p elemetn that wasw just creates 